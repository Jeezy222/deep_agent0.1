# 更新《PR_GIT_规范.md》实施计划

## 目标
- 在根目录文档 `d:\Trae\deep agent主仓库\deep main html\PR_GIT_规范.md` 中，追加并细化以下内容：PowerShell 脚本审查与测试要求、支付模块双重审查与安全清单、虚拟环境/配置的提交约束、统一的提交流程与检查清单、示例提交信息与命令段。
- 保持既有结构与目录，新增章节并更新版本号与最后更新时间。

## 拟新增/更新章节
1. **PowerShell 脚本审查与测试**
   - 错误处理、日志分级、参数化与最佳实践
   - 强制测试用例：为 `start_all_services.ps1` 与 `check_services.ps1` 提供测试点与运行命令示例
2. **支付模块审查（双重审查）**
   - 安全与合规清单：签名校验、鉴权、CORS 收敛、日志脱敏、禁提交密钥
   - 交易流程完整性：发起→网关页→回跳→回调→订阅更新→WS 广播
   - 测试覆盖：后端集成测试/前端契约测试要求
   - 文档同步：更新 `支付/docs/支付系统技术与流程规范.md`
3. **虚拟环境与配置提交约束**
   - `.venv`、`.env`、`.trae` 的 `.gitignore` 策略与敏感信息约束
   - 提交信息需注明影响范围（dev/test/prod）
4. **统一提交流程与检查清单**
   - 提交流程命令示例（status/add/commit/push）与 PR 流程（CI→评审→合并）
   - 预提交与推送前检查清单（代码、测试、文档、日志）
5. **合并策略与回滚/冲突指南**
   - 默认 squash merge；rebase 线性历史；主干保护
   - `git revert`/`git reset --hard`/冲突解决命令片段
6. **示例提交信息集合**
   - 针对脚本、支付、虚拟环境、文档的示例，遵循 `类型(范围): 描述`

## 文档更新方式
- 在现有 TOC 下新增对应小节，并在“版本与历史”处：
  - 版本号：`v1.1.0`
  - 最后更新时间：与实际操作日期一致
  - 重大变更：新增脚本与支付审查规范、测试要求与检查清单

## 不改动与兼容
- 保留原有章节与示例；仅插入新小节并更新目录锚点，避免破坏现有链接。

## 交付
- 提交更新后的 `PR_GIT_规范.md`；内容包含所有新增章节、命令示例与检查清单。

## 验证
- 打开文档检查标题层级、目录锚点、代码块与加粗/斜体强调；确保中文术语统一。
- 对 `start_all_services.ps1`、`check_services.ps1` 的测试要求与命令可复制执行。