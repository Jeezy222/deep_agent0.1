我已完成 PRD 设计，接下来将协调各专家 Agent 按照以下计划执行开发与集成：

### 1. 前端 UI 开发 (Frontend Cloning Expert)
*   **目标**：将截图中的积分卡片封装为独立组件 `CreditsPopover`。
*   **动作**：
    *   在 `frontend/src/components/common` 下创建 `CreditsPopover.tsx`。
    *   使用 CSS Module 实现像素级样式复刻（圆角、阴影、排版）。
    *   修改 `chat.tsx`，将 `CreditsPopover` 集成到“资源点”按钮 (`.topBadge`)，实现点击切换显示/隐藏。

### 2. 后端 API 实现 (Fullstack Engineer)
*   **目标**：支持升级与支付流程。
*   **动作**：
    *   创建 `frontend/src/pages/api/payment` 目录。
    *   实现 `create-order.ts`: 模拟创建支付订单。
    *   实现 `order-status.ts`: 模拟查询支付状态。
    *   更新 `user/credits.ts`: 增加“用户等级”字段以支持“升级”按钮的状态判断。

### 3. 逻辑对接与集成
*   **目标**：联通前后端。
*   **动作**：
    *   在 `CreditsPopover` 中调用 API。
    *   实现点击“升级” -> 模拟支付中 -> 支付成功 -> 刷新积分数据的完整链路。

### 4. 测试与文档 (Testing & Doc)
*   **目标**：验证功能并归档。
*   **动作**：
    *   编写测试用例覆盖支付流程。
    *   生成接口文档和技术集成文档。

待您确认计划后，我将立即开始调度执行。