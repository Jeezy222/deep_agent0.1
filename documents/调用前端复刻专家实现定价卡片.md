我将调用前端复刻专家智能体来帮您实现这个高质量的定价卡片。

### 1. 任务分配
*   **Agent**: `frontend-cloning-expert`
*   **目标**: 像素级复刻“升级到 Manus Pro”弹窗。
*   **输入**: 您提供的参考图片。

### 2. 实施步骤
*   **组件创建**: 在 `frontend/src/components/common` 下创建 `PricingModal.tsx`。
*   **UI 复刻**:
    *   **布局**: 三栏式设计（基础月度、Pro、生产力版）。
    *   **样式**: 圆角卡片、阴影、选中态高亮（中间卡片蓝色边框）。
    *   **细节**: 价格排版、功能列表图标、按钮样式。
*   **交互**: 
    *   Tab 切换（月付/年付）。
    *   点击“开始免费试用”触发升级逻辑。
*   **集成**: 将此新组件集成到 `ChatPage`，替换之前的简单版 `PaymentModal` 或 `IframeModal`。

### 3. 预期输出
*   一个完整的、响应式的 React 组件，包含所有样式和交互逻辑，视觉效果与截图高度一致。

待您确认后，我将立即启动智能体。