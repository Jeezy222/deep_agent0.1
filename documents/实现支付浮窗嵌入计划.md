我已了解您的需求。为了在 `http://localhost:3000/chat` 中以浮窗形式嵌入 `http://localhost:5174/`，我将采取以下实施方案：

### 1. 创建 `IframeModal` 组件
*   **路径**: `frontend/src/components/common/IframeModal.tsx`
*   **功能**:
    *   使用 `iframe` 加载目标 URL。
    *   实现可拖拽功能（基于鼠标事件或 `react-draggable`，考虑到项目依赖，我将优先使用原生鼠标事件实现轻量级拖拽）。
    *   头部包含：标题、最小化 `_`、最大化 `□`、关闭 `×` 按钮。
    *   状态管理：位置 (`x, y`)、尺寸 (`width, height`)、窗口状态 (`normal`, `minimized`, `maximized`)。

### 2. 集成到 `ChatPage`
*   **修改文件**: `frontend/src/pages/chat.tsx`
*   **逻辑**:
    *   引入 `IframeModal`。
    *   修改 `CreditsPopover` 的 `onUpgradeClick` 回调：不再执行 `window.location.href` 跳转，而是设置 `setShowPaymentModal(true)`。
    *   将 `IframeModal` 的 `src` 设置为带有参数的支付 URL (`http://localhost:5174/?userId=...`).

### 3. 样式实现
*   **修改文件**: `frontend/src/styles/chat.module.css`
*   **细节**:
    *   `.iframeModal`: 绝对定位，阴影 `box-shadow: 0 4px 20px rgba(0,0,0,0.15)`，圆角，边框。
    *   `.modalHeader`: 拖拽区域，鼠标样式 `move`。
    *   `.iframeContent`: 宽高 100%。

### 4. 验证与测试
*   验证浮窗是否覆盖在右下角。
*   测试拖拽和调整大小。
*   确认支付流程在 iframe 内是否正常（注意：如果支付模块内部有 `window.top.location` 跳转，可能会跳出 iframe，我们将假设支付模块支持 iframe 嵌入）。

待您确认计划后，我将立即开始编码。