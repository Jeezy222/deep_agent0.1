我已完成对现有代码库的分析，并为您制定了详细的实施计划。

### 1. 确认与配置支付模块路由
*   **动作**: 确认支付模块的前端路由。根据现有文件结构，支付模块的前端位于 `d:\Trae\deep agent主仓库\支付\apps\web`。
*   **计划**: 
    *   在支付模块中确认或创建 `/upgrade` 路由，用于接收升级请求。
    *   确保该页面能够解析 URL 查询参数 (`userId`, `orgId`, `planType`)。

### 2. 前端跳转逻辑实现 (`deep main html`)
*   **修改文件**: `frontend/src/components/common/CreditsPopover.tsx`
*   **逻辑**:
    *   修改 `onUpgradeClick` 处理函数。
    *   不再打开 `PaymentModal`（因为要跳转到独立支付模块）。
    *   构建带参数的 URL：`http://localhost:5173/upgrade?userId=...&orgId=...&planType=PRO`。
    *   使用 `window.location.href` 进行跳转。
    *   添加加载状态 (`isLoading`)，在跳转前显示 loading 指示器。

### 3. 参数传递与安全
*   **参数**: `userId` (来自 Mock API), `orgId` (默认 'org_default'), `planType` ('PRO')。
*   **安全**: 使用 `encodeURIComponent` 对参数值进行编码，防止 URL 格式错误。

### 4. 支付模块接收逻辑 (`支付` 模块)
*   **修改文件**: `apps/web/src/App.tsx` 或新建 `UpgradePage.tsx`。
*   **逻辑**:
    *   使用 `useSearchParams` 获取 URL 参数。
    *   将参数存储到本地状态或 Context 中，用于后续支付初始化接口调用。

### 5. 文档与测试
*   编写对接文档 `frontend/docs/specs/payment-integration.md`。
*   验证从 Chat 页面点击升级 -> 跳转到支付模块页面 -> 参数正确显示的全流程。

待您确认计划后，我将立即开始执行。