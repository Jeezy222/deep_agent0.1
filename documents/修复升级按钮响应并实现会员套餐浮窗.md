通过代码分析，我发现当前的“升级”按钮逻辑虽然包含了模拟支付的 API 调用（`create-order` 和 `order-status`），但它是在按钮内部直接执行“处理中 -> 成功”的状态变化，**并没有弹出用户预期的“会员套餐浮窗”**。这解释了为什么用户感觉“点击后无响应”或“没有跳转到支付模块”。

### 修复与实施计划

我将分两步解决这个问题：

1.  **实现“会员套餐浮窗”组件 (`PaymentModal`)**
    *   创建一个新的 Modal 组件，展示会员套餐详情（如：专业版、企业版）。
    *   包含支付方式选择（模拟）和确认支付按钮。
    *   设计美观的 UI，符合现有风格。

2.  **改造“升级”按钮逻辑**
    *   修改 `CreditsPopover.tsx`。
    *   点击“升级”不再直接触发 API，而是打开 `PaymentModal`。
    *   将原有的 API 调用逻辑（创建订单、轮询状态）移动到 `PaymentModal` 的“确认支付”动作中。

### 具体步骤

1.  **创建组件**：`frontend/src/components/common/PaymentModal.tsx`
2.  **添加样式**：在 `frontend/src/styles/chat.module.css` 中添加 Modal 相关样式（遮罩层、卡片布局）。
3.  **集成组件**：在 `frontend/src/pages/chat.tsx` 中引入 `PaymentModal`，并通过状态控制其显示/隐藏。
4.  **对接逻辑**：确保 `PaymentModal` 能够调用之前的后端 API 完成模拟支付流程。

待您确认后，我将立即执行。