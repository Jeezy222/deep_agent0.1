我已完成对现有代码库的分析，并为您制定了详细的实施计划。

### 核心策略
利用 Next.js 的 API Routes 能力模拟用户信息接口，在不修改 NestJS 后端的前提下完成所有前端功能改造。主要修改集中在 `frontend/src/pages/chat.tsx` 和相关样式文件中。

### 1. 资源点 API 模拟 (Mock API)
*   **创建文件**: `frontend/src/pages/api/user/credits.ts`
*   **功能**: 返回符合截图的 JSON 数据，包含总积分、免费积分、每日刷新量等。
*   **优势**: 前端组件可像调用真实后端一样调用此接口，未来对接真实后端只需修改此文件。

### 2. 页面布局与功能改造 (`chat.tsx`)
*   **布局调整**: 
    *   在 DOM 中交换 `🔔` 图标容器与 `资源点` 容器的位置。
    *   确保 CSS Flexbox 布局能够正确处理新的顺序。
*   **资源点显示**:
    *   使用 `useEffect` 在组件挂载时调用 `/api/user/credits`。
    *   设置 `setInterval` 实现 30秒自动刷新。
    *   格式化显示数值（如 `1,037`）。
*   **管理菜单开发**:
    *   新增 `UserMenu` 组件（或内联 JSX），包含：
        *   头部：头像、昵称、邮箱。
        *   积分卡片：显示详细积分信息和"升级"按钮。
        *   列表项：知识、账户、设置、主页、帮助、退出。
    *   **交互逻辑**:
        *   点击头像 toggle 菜单显示。
        *   点击外部 (`document` 监听器) 或按 ESC 关闭菜单。
        *   添加过渡动画类。

### 3. 样式优化 (`chat.module.css`)
*   **新增样式**:
    *   `.userMenuDropdown`: 绝对定位的菜单容器，带阴影和圆角。
    *   `.menuHeader`, `.menuCreditsCard`: 对应截图中的特定区域样式。
    *   `.menuList`, `.menuListItem`: 菜单项的 hover 效果。
*   **调整样式**:
    *   优化 `.header` 内部元素的间距 (`gap`)。
    *   设置资源点数字为等宽字体 (`font-variant-numeric: tabular-nums`)。

### 4. 响应式与质量保障
*   使用 CSS Media Queries 确保 Header 在小屏幕下自适应（如隐藏部分文字或调整间距）。
*   添加必要的注释和类型定义。

待您确认后，我将立即开始实施。