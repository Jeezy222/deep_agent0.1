我已完成初步的代码分析和任务拆解。为了满足您的需求，我将分阶段进行：

### 1. 支付系统准备
*   **分支管理**: 在 `d:\Trae\deep agent主仓库\支付` 目录下创建新分支 `feature/payment-integration`，确保开发隔离。
*   **接口设计**: 制定详细的 API 规范，包括升级资格校验、支付初始化、支付回调等接口的 JSON 格式和状态码。

### 2. 后端开发 (Fullstack Engineer)
*   **API 实现**:
    *   `POST /api/v1/payment/check-eligibility`: 校验用户是否符合升级条件。
    *   `POST /api/v1/payment/initiate`: 初始化支付流程，生成支付订单。
    *   `POST /api/v1/payment/callback`: 处理支付网关的回调通知。
*   **Mock 数据**: 在真实支付网关对接前，提供 Mock 接口以便前端联调。

### 3. 前端改造 (Frontend Cloning Expert)
*   **清理旧代码**: 移除 `PaymentModal.tsx` 中原有的模拟逻辑和 UI 内容。
*   **新逻辑对接**: 修改 `handleUpgrade` 方法，调用新的后端 API。
*   **交互优化**: 保持“点击升级 -> 检查资格 -> 发起支付 -> 跳转/显示支付结果”的流畅体验。

### 4. 文档与测试
*   **文档编写**: 产出 API 接口文档和系统架构图。
*   **测试执行**: 覆盖单元测试和集成测试，确保支付流程的稳定性和安全性。

待您确认后，我将立即开始执行。